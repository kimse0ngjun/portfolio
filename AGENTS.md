# AGENTS.md

## 프로젝트 목적

이 저장소는 신입 백엔드 개발자의 개인 포트폴리오 사이트를 구축하기 위한 프로젝트다.

특정 기업이나 산업 분야에 종속되지 않고 다양한 채용 지원에 공통으로 제출할 수 있는 포트폴리오를 목표로 한다. 시각적 과시보다 지원자의 실제 경험, 개인 기여, 기술적 판단, 문제 해결 과정을 정확하고 읽기 쉽게 전달하는 것을 우선한다.

## 확정 기술 스택

- Framework: Next.js
- Language: TypeScript
- Styling: Tailwind CSS
- 배포 예정 환경: Oracle Cloud
- 문서 언어: 한국어만 제공
- 테마: 라이트 모드와 다크 모드 모두 지원

프로젝트를 생성할 때는 당시의 안정적인 Next.js 공식 권장 구성을 사용한다. 새로운 라이브러리를 추가하기 전에는 기존 기술로 해결할 수 있는지 먼저 검토하고, 추가가 필요하다면 목적과 유지보수 비용을 설명한다.

## 현재 작업 범위

최종 구현 범위는 Oracle Cloud 배포 직전까지다.

다음 작업은 현재 범위에서 제외한다.

- Oracle Cloud 인프라 생성 및 변경
- 배포 관련 설정 작성
- 실제 배포 수행
- 도메인 및 DNS 연결
- 운영 환경의 비밀값 등록

사용자의 별도 승인 없이 위 범위를 구현하거나 외부 시스템을 변경하지 않는다.

## 승인 게이트

작업은 다음 순서를 따른다.

1. `AGENTS.md` 작성 및 사용자 승인
2. 프로젝트 생성
3. 공통 레이아웃
4. Home
5. About
6. Tech Stack
7. Projects
8. Project Detail
9. Trouble Shooting
10. Contact
11. 반응형 및 접근성 개선
12. 라이트/다크 모드 지원
13. SEO 및 성능 최적화

현재 단계에서는 `AGENTS.md`만 작성한다. 사용자가 이 문서를 승인하기 전에는 프로젝트를 생성하거나 다른 코드, 설정, 패키지, 폴더를 추가하지 않는다.

이후에도 사용자가 작업 범위를 단계별 승인으로 제한하면 해당 승인 범위만 수행한다.

## 콘텐츠 정확성 원칙

- 확인되지 않은 경력, 성과, 수치, 역할, 기술 사용 경험을 임의로 작성하지 않는다.
- 자료가 없는 항목은 사실처럼 채우지 않고 명확한 `TODO` 또는 Placeholder로 남긴다.
- Placeholder는 실제 정보로 오인되지 않도록 화면과 소스에서 분명하게 구분한다.
- 성능 개선 수치는 측정 환경, 비교 기준, 측정 방법이 확인된 경우에만 사용한다.
- 기술 스택은 실제 사용 경험과 근거가 확인된 항목만 최종 콘텐츠로 노출한다.
- 팀 프로젝트에서는 개인 기여와 팀 전체 기여를 명확하게 구분한다.
- 커밋 수나 주관적인 퍼센트만으로 개인 기여를 과장하지 않는다.
- 공개 여부가 확인되지 않은 저장소, 문서, 개인정보, 내부 자료는 노출하지 않는다.
- 사용자가 제공한 원문 자료의 의미를 변경하거나 과장하지 않는다.

## 프로젝트 구성 원칙

홈 요약 페이지와 프로젝트 상세 페이지를 중심으로 구성한다.

홈의 기본 정보 구조는 다음과 같다.

1. Hero
2. About
3. Tech Stack
4. Projects
5. Contact

프로젝트 상세 페이지의 기본 정보 구조는 다음과 같다.

1. 프로젝트 개요
2. 문제 또는 배경
3. 담당 역할
4. 개인 기여와 팀 기여
5. 시스템 아키텍처
6. 주요 구현 내용
7. 트러블슈팅
8. 결과와 배운 점
9. 관련 링크

콘텐츠가 준비되지 않은 섹션은 확인되지 않은 예시로 채우지 않는다. Placeholder로 표시하거나, 사용자와 합의한 경우에만 임시로 숨긴다.

## 대표 프로젝트

대표 프로젝트는 다음 4개이며, 표시 순서는 아래 우선순위를 따른다.

1. BuildSync
2. 의약품 회수 조회 서비스(Medicine-platform)
3. SpecMate
4. VIA

특정 프로젝트 하나만 과도하게 강조하지 않는다. 네 프로젝트를 공통 포트폴리오 용도에 맞게 균형 있게 소개하되, 위 순서는 유지한다.

각 프로젝트는 가능한 한 동일한 정보 구조를 사용한다. 다만 자료가 없는 항목을 억지로 맞추거나 내용을 만들어내지 않는다.

## 링크 및 공개 정보

- 프로필 사진은 사용하지 않는다.
- GitHub 링크를 공개할 수 있게 구성한다.
- 이메일을 공개할 수 있게 구성한다.
- 이력서는 추후 PDF를 추가할 수 있도록 버튼 자리를 준비한다.
- PDF가 제공되기 전에는 버튼이 실제 문서가 있는 것처럼 동작하지 않게 한다.
- 포트폴리오 GitHub 저장소, 프로젝트 GitHub 저장소, 프로젝트 문서 링크를 지원한다.
- 프로젝트 배포 링크는 Oracle Cloud 배포 이후 추가할 수 있는 선택 항목으로 설계한다.
- 값이 없는 선택 링크는 빈 URL이나 동작하지 않는 링크로 렌더링하지 않는다.

## 디자인 원칙

디자인 키워드는 Vercel, Linear, Stripe, Supabase, shadcn/ui다. 이들의 제품을 그대로 복제하지 않고 다음 특성만 참고한다.

- 깔끔하고 전문적인 인상
- 명확한 정보 계층
- 충분한 여백과 읽기 쉬운 타이포그래피
- 절제된 색상과 일관된 디자인 토큰
- 콘텐츠 중심의 카드와 구분선
- 명확한 상호작용 및 포커스 상태

과도한 애니메이션, 장식적 3D 효과, 시선을 방해하는 배경 효과, 불필요한 자동 재생 효과는 사용하지 않는다. 애니메이션이 필요한 경우 의미 전달에 도움이 되는 짧고 절제된 전환만 사용한다.

## 컴포넌트 및 데이터 원칙

- 페이지는 콘텐츠 조합과 라우팅에 집중한다.
- 공통 레이아웃, 섹션, 프로젝트 도메인 컴포넌트, 범용 UI를 구분한다.
- 재사용 가능성만을 이유로 지나치게 작은 컴포넌트로 분리하지 않는다.
- 프로젝트 및 프로필 데이터는 표현 컴포넌트와 분리한다.
- 프로젝트 데이터 구조는 TypeScript 타입으로 명확하게 정의한다.
- 선택 정보는 optional 값으로 모델링하고 값이 없을 때 안전하게 숨긴다.
- Server Component를 기본으로 사용하고, 상태나 브라우저 API가 필요한 부분만 Client Component로 만든다.
- 전역 상태 관리 라이브러리는 실제 필요가 생기기 전에는 추가하지 않는다.
- 백엔드 서버, 데이터베이스, CMS는 현재 정적 포트폴리오 요구만으로 도입하지 않는다.
- 링크는 링크로, 동작은 버튼으로 구현해 HTML 의미를 지킨다.

## 반응형 원칙

- Mobile First로 구현한다.
- 고정 기기 이름보다 콘텐츠가 깨지는 지점을 기준으로 breakpoint를 선택한다.
- 모바일에서 단일 열과 충분한 터치 영역을 제공한다.
- 태블릿과 데스크톱에서는 콘텐츠 양에 따라 열 수를 늘리되 본문 최대 너비를 제한한다.
- 긴 표는 작은 화면에서 카드형 정보 또는 접근 가능한 스크롤 영역으로 처리한다.
- 시스템 아키텍처는 작은 화면에서도 확인할 수 있게 확대 또는 대체 설명을 제공한다.
- 가로 스크롤과 레이아웃 이동을 최소화한다.

## 접근성 원칙

- 시맨틱 HTML과 올바른 제목 계층을 사용한다.
- 본문 바로가기 링크를 제공한다.
- 모든 핵심 기능은 키보드로 사용할 수 있어야 한다.
- 포커스 상태를 명확하게 표시한다.
- 색상에만 의존해 의미를 전달하지 않는다.
- 텍스트와 UI 색상은 WCAG AA 수준의 대비를 목표로 한다.
- 정보 이미지에는 적절한 대체 텍스트를, 장식 이미지에는 빈 대체 텍스트를 사용한다.
- 아이콘 단독 버튼에는 접근 가능한 이름을 제공한다.
- 모바일 메뉴는 상태, 포커스, 키보드 동작을 올바르게 관리한다.
- `prefers-reduced-motion` 설정을 존중한다.
- 문서 기본 언어를 한국어로 선언한다.
- 다이어그램에는 핵심 구조를 설명하는 텍스트 대안을 제공한다.
- 자동 접근성 검사만으로 완료하지 않고 키보드 탐색을 직접 확인한다.

## 라이트/다크 모드 원칙

- 두 테마에서 정보 계층과 명도 대비가 유지되어야 한다.
- 색상은 직접 반복하지 않고 의미 기반 디자인 토큰으로 관리한다.
- 시스템 테마를 초기 기본값으로 존중하고 사용자가 선택을 변경할 수 있게 한다.
- 사용자가 선택한 테마는 가능한 범위에서 유지한다.
- 초기 렌더링에서 불필요한 테마 깜빡임을 최소화한다.
- 테마 전환은 장식이 아닌 명확한 이름과 키보드 조작을 제공한다.

## SEO 및 성능 원칙

- 각 페이지에 실제 콘텐츠에 맞는 고유한 제목과 설명을 제공한다.
- 프로젝트 상세 페이지에 프로젝트별 메타데이터를 제공한다.
- Open Graph 등 공유 메타데이터를 준비한다.
- 사이트맵과 robots 설정은 실제 공개 경로가 확정된 후 구성한다.
- 의미 있는 제목 구조와 내부 링크를 유지한다.
- 불필요한 Client Component와 JavaScript를 줄인다.
- 이미지 크기와 형식을 최적화하고 레이아웃 이동을 방지한다.
- 폰트는 성능과 한글 가독성을 함께 고려한다.
- 성능 목표는 측정 결과를 기반으로 개선하며 확인되지 않은 점수를 보장하지 않는다.

## 코드 품질 원칙

- TypeScript의 타입 검사를 우회하기 위해 `any`를 습관적으로 사용하지 않는다.
- 불가피한 타입 단언에는 근거가 있어야 한다.
- 컴포넌트 이름과 데이터 필드는 역할이 드러나게 작성한다.
- 중복을 줄이되 성급한 추상화를 피한다.
- 오류와 빈 상태를 고려한다.
- 환경별 값과 공개 가능한 값을 구분한다.
- 비밀값이나 개인정보를 저장소에 커밋하지 않는다.
- 주석은 코드가 무엇을 하는지 반복하기보다 선택 이유나 제약을 설명할 때 사용한다.
- 기존 사용자 변경 사항을 임의로 삭제하거나 덮어쓰지 않는다.

## 검증 원칙

각 구현 단계에서는 변경 범위에 맞춰 다음을 확인한다.

- TypeScript 타입 검사
- lint 검사
- production build
- 주요 링크와 경로
- 모바일 및 데스크톱 레이아웃
- 키보드 탐색과 포커스 상태
- 라이트/다크 테마 대비
- 콘텐츠의 사실 여부와 Placeholder 구분
- 브라우저 콘솔 오류

테스트 또는 검증을 실행하지 못한 경우 실행한 것처럼 보고하지 않고, 미실행 항목과 이유를 명시한다.

## 작업 및 보고 원칙

- 요청받은 범위를 넘어서는 큰 변경을 임의로 수행하지 않는다.
- 구현 전에 현재 저장소 상태와 사용자 변경 사항을 확인한다.
- 패키지를 추가할 때 선정 이유와 대안을 설명한다.
- 기존 파일을 수정할 때 관련 없는 사용자 변경을 보존한다.
- 완료 보고에는 변경 내용, 검증 결과, 남은 TODO를 구분해 전달한다.
- 실제 자료가 필요한 지점은 구체적인 요청 목록으로 사용자에게 알린다.
- Oracle Cloud 배포 관련 작업은 별도 승인 전까지 진행하지 않는다.
